import 'package:flutter/material.dart';

class ShipmentCardRow extends StatelessWidget {
  const ShipmentCardRow({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
